# FCP-COURSEWORK
Run the main function in terminal, with the named flags in format '-flag' or for the Hint flag's number just '10' for example.
Profile will output plots when the -file flag is NOT present, as it cannot output plots to .txt files.

